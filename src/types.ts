type worksData = {
  card__image: string,
  card__heading: string,
  card__info: string,
  card__skills: string[],
  href: string
}

type fullInput = {
  subject_input: boolean,
  name_input: boolean,
  phone_input: boolean,
}